{% extends 'base.html.twig' %}

{% block body %}
    <section  class="container mt-3">
        <h2>Demande d'inscription à l'AMAP</h2>
        <p class="font-weight-bold text-warning bg-dark ps-2">
        Toute personne voulant souscrire un contrat doit s'inscrire à l'Amap et être à jour de sa cotisation.</br>
        La demande d'inscription ci-dessous sera enregistrée et le compte sera en attente d'activation jusqu'à validation par un responsable.
        </p>
        <p>
            <a href="{{ path('app_login') }}" class="btn btn-secondary mt-3">Si déjà inscrit -> se connecter</a>
            <a href="{{ path('app_main') }}" class="btn btn-secondary mt-3">Annuler (retour à l'accueil)</a>
        </p>

        {{ form_start(registrationForm) }}
            <fieldset class="mb-3">
                <legend>Mon identité</legend>
                {{ form_row(registrationForm.nom) }}
                {{ form_row(registrationForm.prenom) }}
                {{ form_row(registrationForm.email) }}
            </fieldset>
            <fieldset class="mb-3">
                <legend>Mes coordonnées</legend>
                {{ form_row(registrationForm.adresse) }}
                {{ form_row(registrationForm.code_postal) }}
                {{ form_row(registrationForm.commune) }}
                {{ form_row(registrationForm.tel_portable) }}
                {{ form_row(registrationForm.tel_2) }}
            </fieldset>
            
            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe'
            }) }}
            {{ form_row(registrationForm.RGPDConsent) }}

            <button type="submit" class="btn btn-primary my-3">Demande inscription</button>
        {{ form_end(registrationForm) }}
    </section>
{% endblock %}
