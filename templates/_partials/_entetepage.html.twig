<nav class="navbar navbar-expand bg-success text-white">
  <div class="container-fluid">
    <h3>AMAP St Sébastien sur Loire</h3>
    <ul class="navbar-nav ms-auto">
        {% if app.user %}
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle text-warning"
                        role="button"
                        data-bs-toggle="dropdown"
                        aria-expanded="false">
              {{ app.user.prenom }} {{ app.user.nom }}  
            </a>
            <ul class="dropdown-menu dropdown-menu-end text-white">
                {% if app.user.isIsValide() %}
                  <li><a class="dropdown-item" href="#">mon compte</a></li>
                  <li><a class="dropdown-item" href="#">mes contrats</a></li>
                  <li><a class="dropdown-item" href="#">nouveau contrat</a></li>
                {% else %}
                  {% if app.user.isIsMailValide() %}
                    <li><a class="dropdown-item " href="#">mon compte</a></li>
                    <li><a class="dropdown-item disabled" href="#">mes contrats</a></li>
                    <li><a class="dropdown-item disabled" href="#">nouveau contrat</a></li>                    
                  {% else %}
                    <li><a class="dropdown-item" href="#">mon profil</a></li>
                  {% endif %}
                {% endif %}
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">me déconnecter</a></li>
            </ul>
          </li>
        {% else %}
          <li class="nav-item">
            <a href="{{ path('app_login') }}" class="nav-link text-white" data-toggle="tooltip" title="si vous êtes déjà inscrit à l'AMAP">se connecter</a>
          </li>
          <li class="nav-item">
            <a href="{{ path('app_register') }}" class="btn btn-warning" data-toggle="tooltip" title="si vous voulez vous inscrire à l'AMAP">s'inscrire</a>
          </li>
        {% endif %}
    </ul>
  </div>
</nav>